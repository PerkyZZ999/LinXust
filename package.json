{
  "name": "linxust",
  "private": true,
  "version": "0.1.0",
  "description": "LinXust Electron + Rust napi-rs archiver scaffold",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build && cargo build --manifest-path native/Cargo.toml",
    "test": "cargo test --manifest-path native/Cargo.toml",
    "electron": "electron ."
  },
  "dependencies": {
    "electron": "33.2.1"
  }
}
